node_modules
.next
.env*.local
.env
*.log
.vercel
tsconfig.tsbuildinfo

.vercel

# Playwright
/playwright/.auth/
/playwright/.cache/
/playwright-report/
/test-results/
/blob-report/
/e2e-results.json

# Scratch clone of @fchecklist/veridian-ui-kit used to prepare the shared-
# package side of the left-rail column-toggle fix (task-20260816-172825) --
# not a projexa source directory, never intended to be committed here.
/.ui-kit-work/
/.visual-evidence/

# Local bun toolchain + real-app verification scripts/evidence
# (task-20260817-024638-make-the-composer-visible-without-scroll) -- not
# projexa source, never intended to be committed here.
/.bun/
/.run/
.env*

# R67 F-28: perf-harness.mjs OUTPUT (perf/budgets.json itself is committed).
# One machine's numbers are not another's, so the measured table is an
# artifact, never a repo file.
/perf/perf-results.json
/perf/perf-results.md
