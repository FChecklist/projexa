{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": [
    "bom1"
  ],
  "ignoreCommand": "sh -c 'if [ \"$VERCEL_ENV\" = \"production\" ]; then exit 1; else exit 0; fi'",
  "crons": [
    { "path": "/api/internal/email-digest-cadence/run", "schedule": "0 8 * * *" }
  ]
}
