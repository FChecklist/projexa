# ============================================================================
# ACTIVE CLAIMS REGISTRY -- read this BEFORE picking any new gap/task to work
# on in this repo. Bootstrapped 2026-07-19 for PROJEXA, following the exact
# same convention established in FChecklist/compliance-tracker's own
# ai-os/boss/ACTIVE-CLAIMS.yaml (created there 2026-07-14). PROJEXA did not
# have this file before this commit -- AGENTS.md already disclosed PROJEXA
# has "no ai-os/ directory" and no dispatch infrastructure of its own; this
# is that gap's first real piece, added because this task is the first time
# a session needed to claim file-scoped work in this repo ahead of a
# multi-tenant schema change. Not claiming any pre-existing history this file
# never recorded.
#
# THIS FILE IS NOT A SUBSTITUTE FOR GIT'S OWN SAFETY (worktree isolation +
# PR/CI + branch protection already prevent silent data loss). It exists to
# prevent a DIFFERENT problem git can't solve on its own: two sessions
# independently choosing to build the SAME gap at the same time, wasting a
# full agent-hours cycle on duplicate/conflicting work that then has to be
# thrown away or painfully reconciled after the fact.
#
# PROTOCOL (every session, every time, no exceptions):
#   1. BEFORE selecting a gap/task to work on: `git pull origin main` this
#      file fresh, read every entry under `active:` below, and do NOT start
#      work on a gap/file-area another entry already claims, unless that
#      entry's `claimed_at` is more than 4 hours stale with no matching open
#      PR (check `gh pr list`) -- treat a stale claim as abandoned, not
#      binding.
#   2. IMMEDIATELY when you start real work (dispatching a sub-agent, or
#      starting to edit yourself): add a new entry under `active:` with your
#      session label, what you're claiming (gap id + real file/directory
#      scope, not just a vague area), and a timestamp. Commit and push this
#      addition on its own, fast, BEFORE the actual work PR -- don't batch it
#      with the real change, so other sessions see the claim as early as
#      possible.
#   3. WHEN your work merges (or you abandon it), move your entry from
#      `active:` to `recently_completed:` (keep the last ~15 entries there,
#      drop older ones -- this is a rolling log, not permanent history).
#   4. If you discover an active claim that conflicts with what you were
#      about to do, do NOT silently work around it or silently pick a
#      different gap without saying so -- note it in your own session's
#      final report to your Owner/user.
#   5. Sessions cannot literally message each other -- this file IS the
#      message. Keep entries short, factual, and file/directory-scoped so a
#      session with zero other context can act on them correctly.
# ============================================================================
active:
  - session_label: "claude-code (interactive, isolated worktrees C:/ct/ct-worktrees/wo-exceptions-fix + C:/ct/projexa-worktrees/wo-exceptions-fix, branch wo/exceptions-drilldown-navigation + wo/exceptions-item24-and-navigation) -- PROJEXA-E2E-001: direct fix (not deferred) for the 2 real bugs found by the R-92..R-100/31-vs-28 exceptions reconciliation session (2026-09-20/21) -- Bug 1 (compliance-tracker, read-only here): construction-exceptions-service.ts getProjectExceptions() 29-vs-28 array-length bug; Bug 2 (this repo): ExceptionsClient.tsx had zero drill-down navigation from a flagged exception to the real record it names [DONE -- both fixed, unit/component tests green locally, PRs open/unmerged pending real CI e2e verification]"
    claimed_at: "2026-09-21T (this session)"
    completed_at: "2026-09-21T (this session)"
    claim: |
      File scope this repo: src/components/ExceptionsClient.tsx + its
      .test.tsx sibling. Added recordHref() consuming a new recordType/
      linkId field the companion compliance-tracker fix adds to each
      ExceptionRecord, mapping each flagged record to its real object
      screen (/change-orders/[id], /scope/[id], /site-diary/[id],
      /work-progress/[id], /labour/[id], /punch-list/[id], /materials/[id]
      via linkId=materialId, /invoices?highlight=[id] for interim bills --
      same pattern BillingMilestonesClient.tsx's own "View invoice" action
      already uses). 3 record types (vendor disputes, customer complaints,
      subcontractor invoice lines) and the date-only #20/#26 rows have no
      real per-record screen in PROJEXA today -- left as plain text, not a
      fabricated link, documented honestly in the component's own header
      comment. Full detail (including the sandbox network constraint that
      shaped this session's verification strategy) in compliance-tracker's
      own ACTIVE-CLAIMS.yaml entry for this same work.
  - session_label: "claude-code (sub-agent, PROJEXA-E2E-001 work order section 4) -- systemic sweep: EVERY GET /reports/{name} consumer for the R67 E-32 format=legacy gap, plus dashboard/analytics silent-wrong-shape audit, traceability, cross-role reconciliation, empty/partial/heavy states"
    claimed_at: "2026-09-20T (this session)"
    claim: |
      Scope: both repos, isolated worktrees (C:\ct\projexa-reports-sweep,
      branch fix/reports-systemic-format-legacy-sweep off origin/main @
      090139b7; compliance-tracker worktree only if a backend fix turns out
      to be needed -- so far every finding is projexa client-side). This is
      the FOLLOW-THROUGH task after PR #300 (fix/reports-project-status-
      dashes, still open/unmerged as of this claim) fixed reportDestination()
      centrally (covers every report reached via the Reports picker) plus
      ReportsClient.tsx's budget breakup fetch -- this claim is specifically
      for OTHER client call sites that build `/api/reports/{name}` URLs
      directly, bypassing reportDestination(), and therefore still miss
      format=legacy.

      Already found via static read (grep for every /api/reports/ fetch site
      in src/, cross-checked against compliance-tracker's REPORT_REGISTRY in
      construction-reports-service.ts and the [reportName]/route.ts table-vs-
      legacy switch): 8 more call sites read the handler's own payload shape
      directly with no format=legacy, so each now silently gets the R67 E-32
      default table shape instead --
      src/components/BudgetAnalyticalClient.tsx:107 (budget-variance, Scope
      screen's Cost Variance/budget tab), src/components/BudgetActualClient
      .tsx:66 (budget-variance, Scope screen's Revenue/Budget/Actual tab),
      src/components/DashboardProjectClient.tsx:346 (category-progress,
      legacy-VERIDIAN fallback path), src/lib/work-progress-reads.ts:166
      (category-progress, feeds the Work Progress Analytics category bars --
      one of the 5 analysis-screens.ts entries), src/components/
      DesignStudioTimesheetClient.tsx:148 (designer-approval-status),
      src/components/DesignStudioCostAnalysisClient.tsx:51
      (designer-timesheet, also an analysis-screens.ts entry, reached from
      the Reports picker's "Designer Timesheet" hosted-report navigation),
      src/components/LabourDailySummaryClient.tsx:131
      (manpower-daily-summary), src/lib/google-sheets/push.ts:134+171 (ANY
      report name a user types into the Sheets integration's Reports tab, or
      every ANALYSIS_REPORT_NAMES entry -- feeds a generic flattener built
      for the handler shape). Confirmed each of the first 7 reads a specific
      handler field (`data.contractValue`, `body.categories ?? []`,
      `report.lines`, etc.) that does not exist on the table shape, so each
      silently renders empty/undefined rather than erroring -- exactly the
      "no dashes where data exists" class the work order names. boq-analysis
      consumers (ChangeOrderObjectClient, BillingMilestonesClient,
      Project360Client, WorkspaceBoqCard) are NOT affected -- confirmed
      boq-analysis is a standalone compliance-tracker route
      (/v1/projexa/reports/boq-analysis), never routed through the
      [reportName] dynamic dispatcher R67 E-32 changed.

      [DONE, 2026-09-20 same session] PR opened: FChecklist/projexa#306,
      CI green (Lint/Type Check/Test/Build/Migration guard/Secret Scanning
      all pass; "Vercel: Deployment was blocked" is the R87 ignoreCommand
      gate working as designed on a non-main branch, not a failure), left
      OPEN/UNMERGED per the owner's ground rules.

      Remaining audit items, completed by static/code analysis rather than
      live UI (see below for why): the other 4 analysis-screens.ts entries
      (category-distribution already had format=legacy via the pre-existing
      category-distribution proxy routes; project-360 is boq-analysis-based,
      unaffected; exceptions calls /api/exceptions, not /api/reports/, also
      unaffected) -- no further silent-null class bugs found by code
      inspection. Traceability: real drill-down exists on exactly 2 of ~22
      reports -- BudgetActualClient.tsx and BudgetAnalyticalClient.tsx both
      link a BOQ line/code to /scope/{boqId}[#line-{id}] -- but
      ProjectStatusCard.tsx (the Project Status card PR #300 fixed) and
      ReportOutput.tsx (the generic renderer used for ~16-18 of the 22
      REPORT_REGISTRY reports shown inside the Reports frame) have ZERO
      Link/href anywhere -- a real, significant, work-order-relevant gap,
      reported rather than shortcut-fixed (building drill-down for 16+
      reports is a real feature, not a quick fix). Cross-role: the ONLY
      role-based figure gate found anywhere in REPORT_REGISTRY is
      "budget-vs-actual" -> requires manager+ (construction-reports-service
      .ts/[reportName]/route.ts, explicitly commented as deliberate, R48
      gap-closure); every other report/dashboard computes identically
      regardless of caller role, and the Reports picker UI itself has zero
      role-based filtering -- so the only cross-role divergence in this
      product is that one documented, deliberate gate (a clean 403 with the
      backend's own words, not a silently different number), not an
      accidental query-filter bug. Empty/partial/heavy states: confirmed via
      direct Supabase query (pcrjmlpuqsbocqfwoxod) real candidates exist in
      BOTH orgs -- projexa_demo_org's "Villa 21 - Whitefield" (164 BOQ
      lines/35 progress entries, NOT ~2,467 as an earlier session's report
      claimed -- recounted directly, worth someone reconciling that number)
      is the heavy project; org 4ecc472f-4152-4310-ae8d-cf8b7c52ab6d
      (Meridian Construction Group, e2e/users.ts's own org) has an even
      heavier "Meridian Heights - Residential Tower A" (5,102 BOQ
      lines/611 progress entries), several 12-line/0-progress partial
      projects, and multiple genuinely empty (0/0) "E2E-BatchA-*" projects
      -- all confirmed to exist, ready for a future session's live UI pass.

      LIVE UI VERIFICATION WAS BLOCKED, honestly reported rather than
      faked: this machine hit 0.06 GB free RAM mid-session (multiple
      concurrent sessions sharing it per this file's own other active
      entries), and the already-running shared dev server at :3100
      genuinely, repeatedly failed with "The construction data service did
      not respond in time" on every project-load attempt (a live instance
      of the already-documented R83 construction-data-service timeout, not
      a defect introduced by this task -- a direct fetch() from the page
      itself hung past 45s). Did not spin up a second dev server instance
      given the RAM state. So: the format=legacy fixes are proven by
      falsifiability-tested unit tests + direct backend-source reading (not
      a live click), and the dashboard/traceability/cross-role/empty-state
      findings above are from live database + static code analysis, not a
      live browser session against real UI. A future session with more
      headroom should do the live-browser pass this one could not safely
      attempt.
  - session_label: "claude-code (sub-agent, PROJEXA-E2E-001 sub-task) -- fix /site-diary 'Empty while Work Progress holds the same history' (work order section 5 item 3)"
    claimed_at: "2026-09-20T (this session)"
    claim: |
      Worktree: C:\ct\projexa-sitediary-fix (branch fix/site-diary-empty), off
      origin/main. Isolated from the shared C:\ct\projexa checkout other
      parallel sessions are using.

      ROOT CAUSE: same bug class already fixed for /change-orders (PR #299),
      NOT the /reports response-shape-mismatch class (PR #300), and NOT a
      backend/data gap. compliance-tracker's construction-site-diary-
      service.ts's listSiteDiaries(), called directly against the live
      Supabase project (bypassing HTTP/auth), correctly returns 5 real rows
      for org=projexa_demo_org / project=projexa_demo_project ("Villa 21 -
      Whitefield"), alongside 35 real work-progress entries for the same
      project -- the table and the query were never empty or wrong. The real
      defect: site-diary/page.tsx never named which project it was showing.
      resolveSelectedProject()'s documented, deliberate first-project
      fallback (project-selection.ts) silently picks the org's
      alphabetically-first project when nothing is asked for -- "Business Bay
      Corporate HQ", which has zero seeded site-diary entries AND zero
      work-progress entries. /work-progress reaches its project differently
      (module-list-source.ts's resolveProjectForModule, R67 F-18): it honours
      an explicit ?projectId= from a project-scoped link without writing the
      rail's remembered-project cookie, so a visit to /work-progress?projectId=
      <villa21> can show real history while a later, plain sidebar click to
      /site-diary (no ?projectId=, same session, cookie still unset) silently
      falls back to the empty project -- the exact asymmetry reported.

      FIX: adopted the same project-naming + "(auto-selected)" admission
      already shipped for Documents/Labour/Drawings/MoMs/change-orders (R67
      D-13/D-20/D-32 convention) -- site-diary/page.tsx now passes
      project.name/fellBack through to PageHeading and SiteDiaryClient, and
      the empty state names the project and, when auto-picked, says so and
      points at the project switcher.

      Tests: SiteDiaryClient.test.tsx (new, 4 cases -- falsifiability-checked
      by reverting the empty-state fix, confirming 2 of 4 fail, then
      restoring). Full existing suite (4293 tests) still green. Typecheck and
      lint both clean on the changed files.

      File scope: src/app/(app)/site-diary/page.tsx,
      src/components/SiteDiaryClient.tsx,
      src/components/SiteDiaryClient.test.tsx (new), this file (claim entry).
      Did NOT touch compliance-tracker (no backend change needed -- the query
      and data were already correct; see that repo's own ACTIVE-CLAIMS.yaml
      for this sub-task's read-only-verification entry there).
  - session_label: "claude-code (sub-agent, PROJEXA-E2E-001 sub-task) -- investigate + fix PROJEXA /reports Project Status Report showing dashes against a project with real BOQ/progress figures (work order section 5 item 2, called out again in section 4 as the single worst-weighted finding)"
    claimed_at: "2026-09-20T (this session)"
    claim: |
      Scope: both repos, isolated worktrees (C:\ct\projexa-reports-fix,
      C:\ct\ct-reports-fix), branch fix/reports-project-status-dashes in
      both. Checking first whether this is the same project-resolution
      class of bug /change-orders had (PR #299, sibling fix, same work
      order section 5 item 1) before assuming a genuine data/rendering bug
      -- resolveSelectedProject()/resolveProjectForModule() is already in
      the /reports chain (src/lib/module-list-source.ts), so verifying
      directly against live Supabase (pcrjmlpuqsbocqfwoxod) data for
      projexa_demo_org/projexa_demo_project rather than assuming either
      way. Likely touches src/app/(app)/reports/page.tsx,
      src/components/ReportsClient.tsx,
      src/components/reports/ProjectStatusCard.tsx, src/lib/report-run.ts
      here, and/or src/lib/services/construction-reports-service.ts /
      construction-dashboard-service.ts in compliance-tracker. Will update
      this claim with the real finding once confirmed. Per owner's ground
      rules: local bun dev + live Supabase + GitHub CI only, never Vercel;
      PR(s) opened, CI green, left OPEN/UNMERGED.
  - session_label: "claude-code (interactive, Claude Desktop, PM session) -- WORK ORDER PROJEXA-E2E-001: verify all 111 requirements/4 surfaces/every role, local+GitHub+Supabase only, no Vercel deploy until owner approves"
    claimed_at: "2026-09-20T (this session)"
    claim: |
      Owner work order PROJEXA-E2E-001 (pasted in chat, not a file on disk -- searched
      Desktop/Downloads/Documents/OneDrive/both repos, does not exist; treating the
      pasted text as source of truth and writing it to PROJEXA-E2E-001.md at this
      repo's root as the durable record). DPDP work in compliance-tracker is PAUSED
      per the owner's own instruction until this closes.

      Order (owner's own section 8): (1) re-verify the 14 rows in
      platform.sumeet_requirements (Supabase pcrjmlpuqsbocqfwoxod) that allegedly cite
      Playwright e2e/ tests that don't exist -- re-open and write real tests, re-close
      only what genuinely passes; (2) fix the 8 broken screens named in section 5,
      starting with /change-orders; (3) reports/dashboards (heaviest weight); (4)
      requirement-by-requirement, 4 surfaces (one-page/ERP/AI-link/email), every role;
      (5) the cold-load (10-20s) investigation.

      Ground rules from the owner, binding for the whole work order: local `bun dev`
      + GitHub CI + live Supabase only. NO Vercel preview/branch deploys "just to
      check" -- Vercel gets exactly one deployment, at the very end, only after
      everything is green AND the owner has explicitly said so. External reachability
      (AI link pasted into a chat engine, email links clicked from a real inbox) uses
      a cloudflared tunnel to the local dev server, not a Vercel preview. Also
      confirming/fixing the Vercel "Ignored Build Step" on both projects
      (veridian-compliance-ai and projexa) so only `main` can trigger a build --
      read-only check first, reporting before any change, per the owner's explicit
      "report the commit" instruction once applied.

      Initial finding already contradicts the owner's own count and is being verified
      further before reporting: all e2e/ files cited by the 20 e2e-citing rows in the
      register (across both repos' e2e/ directories) DO exist on origin/main by simple
      path check -- "phantom" must mean something subtler (file exists but doesn't
      contain a real assertion for that specific requirement id, is excluded from the
      CI job that would actually run it, is skipped/fixme, or the cited CI run/commit
      doesn't check out) -- not reporting a false "0 phantom" until this is confirmed
      file-by-file, test-by-test.

      File/scope: e2e/ in both repos (read/write), the 8 named broken screens (mostly
      projexa src/app/(app)/*), platform.sumeet_requirements (PM-only writer per its
      own comment), vercel.json in both repos (Ignored Build Step only, not touching
      the existing R87 main-branch gate logic without checking it first),
      PROJEXA-E2E-001.md (new, this repo root). NOT touching compliance-tracker's
      DPDP work (explicitly paused by the owner for the duration of this order).
  - session_label: "claude-code (interactive, Claude Desktop) -- org-configurable start-of-day/end-of-day email digest, actionable by reply (Owner directive 2026-09-19). Follow-up on WO-PROJEXA-AI-LINK-001's own deliberately-deferred scope (digest.ts's header: 'does NOT implement the mandatory cadence engine... or a cron trigger' -- that comment is now partly stale, a fixed single daily cron exists, but per-org schedule/timezone, morning/evening split, and inbound reply parsing genuinely do not)."
    claimed_at: "2026-09-19T (this session)"
    claim: |
      Owner directive: every PROJEXA user should get 1-3 emails/day (org-configurable
      frequency and local time, default 09:00/18:00) covering their open todos plus
      org-wide open RFIs/submittals/punch-list/billing-milestones, actionable either by
      one-click link (existing mechanism, unchanged) or by replying to the email in
      plain text (new). Working in an isolated worktree at
      C:\ct\projexa-email-digest, branch feat/org-email-digest-schedule, off
      origin/main @ f4d1d9b -- NOT touching the feat/merge6-project-workspace branch's
      uncommitted work sitting in the main C:\ct\projexa checkout.

      File/scope: new migration under drizzle/ (organizations.timezone column;
      new tables org_email_schedule, email_digest_run, email_digest_delivery,
      email_digest_item, daily_report_note -- all additive, existing
      email_action_token/emailActionToken one-click flow left untouched),
      src/lib/db/schema.ts (additive), src/lib/email/schedule-service.ts (new),
      src/lib/email/digest.ts (extended, not replaced -- runDigestCadence becomes
      schedule-aware), src/lib/email/reply-parser.ts (new),
      src/lib/services/digest-item-dispatcher.ts (new), a small extraction of shared
      service functions out of the existing src/app/api/{rfis,submittals,
      punch-list,billing-claims}/[id]/route.ts PATCH handlers (so the dispatcher and
      the authenticated route share logic instead of duplicating it),
      src/app/api/email/inbound/route.ts (new, public, Postmark webhook),
      src/app/api/organization/email-schedule/route.ts (new),
      src/components/SettingsClient.tsx (new "Daily Digest" card),
      src/app/api/internal/email-digest-cadence/run/route.ts (unchanged endpoint,
      now idempotent per org/slot/local-date so both the existing vercel.json cron
      and a new GitHub Actions poller can safely hit it redundantly),
      .github/workflows/email-digest-poll.yml (new, */15 cron), a new
      HANDOFF_DNS_POSTMARK_INBOUND.md (owner-facing DNS/Postmark-account handoff,
      same style as the existing HANDOFF_DNS_RESEND.md -- inbound needs a second
      provider since Resend is send-only, confirmed in that file).

      Deliberately deferred (named, not silently dropped): change-order actions and
      billing-milestone invoice (need structured payloads unsafe to free-text-parse),
      work-progress entry creation (existing offline-queue UI already covers this),
      true per-assignee filtering (no real assignee field exists on
      RFIs/submittals/punch-list/change-orders today -- confirmed by reading their
      schemas/UI), and AI/LLM-assisted reply parsing (deterministic parsing +
      unmatched-text-becomes-a-report-note only, for Phase 1 safety).

      Vercel/DNS: nothing deployed to Vercel or changed in DNS by this session --
      code-complete + locally/Supabase-verified only, per the owner's standing
      local+Supabase+GitHub-first policy; PR opened for review, not pushed to main.
    claimed_at: "2026-09-18T (this session)"
    claim: |
      Phase 1 (change-orders bug, item 5): investigated, no code change
      needed -- see compliance-tracker's entry for the full finding.

      Phase 2 (milestones, item 2): new /api/milestones + /api/milestones/[id]
      proxies, new MilestonesClient.tsx, wired as a 5th Schedule tab
      (src/lib/schedule-tabs.ts, src/components/ScheduleTabsClient.tsx --
      updated its own test's hardcoded 4-tab assertion to 5).

      Phase 3 (Project 360 Analysis, items 6/7/8): new
      /api/reports/boq-analysis + /api/billing-claims proxies, new
      src/components/Project360Client.tsx, new page
      src/app/(app)/analysis/project-360/page.tsx, listed from the existing
      /analysis screen (src/lib/analysis-screens.ts, now 5 entries --
      updated analysis-screens.test.ts) alongside its other 4 analytical
      screens. New route registered in src/lib/nav-routes.ts's
      SHIPPED_ROUTES and in nav-routes.test.ts's
      ROUTES_INTENTIONALLY_NOT_IN_NAV allowlist (reached via /analysis's own
      list, same class as /dashboard/project -- not a duplicate top-level
      sidebar entry).

      File scope: src/app/api/milestones/route.ts + [id]/route.ts,
      src/app/api/reports/boq-analysis/route.ts, src/app/api/billing-claims/
      route.ts, src/components/MilestonesClient.tsx (+test),
      src/components/Project360Client.tsx (+test), src/lib/schedule-tabs.ts,
      src/components/ScheduleTabsClient.tsx (+test update),
      src/lib/analysis-screens.ts (+test update), src/lib/nav-routes.ts,
      src/lib/nav-routes.test.ts.
  - session_label: "claude-code (interactive, Claude Desktop, PROJEXA-DECK-001) -- full PROJEXA user-manual PowerPoint with real local screenshots, per Owner work order dated 2026-09-18"
    claimed_at: "2026-09-18T (this session)"
    claim: |
      Owner work order PROJEXA-DECK-001 (Downloads\PROJEXA-DECK-001.md, from
      raajat.agarwal@gmail.com): a complete, non-technical, click-by-click user
      manual for projexa-ai.com as a PowerPoint (100-150 slides), for Sumeet's
      team. Production (projexa-ai.com) is 503 DEPLOYMENT_PAUSED, so per the
      work order's own explicit instruction this is captured from the LOCAL dev
      server (bun run dev, port 3100) against the real evpckeuxgvahguwsaeul
      Supabase project -- NOT from Vercel, and nothing is deployed to Vercel as
      part of this task.
      File/data scope, this repo: `_deck_capture.mjs` at repo root (a one-off,
      untracked Playwright screenshot-capture script, deleted when the deck is
      done -- not meant to be committed), 3 new Supabase Auth users + memberships
      added to the EXISTING org "Skyline Builders (PROJEXA Demo)"
      (organizations.id 6dfebbde-3ded-4df5-aebd-64f85b7d9e85, already bridged via
      veridian_credentials to compliance-tracker's rich pre-seeded
      'projexa_demo_org' / 'projexa_demo_project' construction dataset): CEO
      (ceo@demo.projexa-ai.com, role owner), Demo PM (pm@demo.projexa-ai.com,
      role pm), Demo Site Engineer (site@demo.projexa-ai.com, role
      site_engineer) -- password not recorded here, session-local only. Did NOT
      remove the org's 2 pre-existing accounts (suresh.pillai/arjun.mehta). NOT
      touching any application source file under src/, drizzle/, or any other
      PROJEXA code -- this is a data-and-screenshot task, not a build task. If
      screens turn out broken/unfinished during capture, the work order's own
      instruction is to screenshot and report them, NOT fix them here (fixing is
      explicitly out of scope for this task). See compliance-tracker's own
      ACTIVE-CLAIMS.yaml for that repo's (passive, dev-server-only) role.
  - session_label: "claude-code (interactive, Claude Desktop, ct-ca session) -- WO-PROJEXA-AI-LINK-001: AI Link + email-as-interface"
    claimed_at: "2026-09-16T (this session)"
    claim: |
      Owner directive relayed from the VERIDIAN-AIOS.COM (compliance-tracker) session,
      confirmed directly by the Owner in this session: build (1) a read-only "AI Link"
      per-(org,user) token -> public GET route -> SQL SECURITY DEFINER STABLE projection
      function (personal-data exclusion enforced at the DB layer, not app code) -> a
      verb-allowlisted proposal-apply flow (ASSIGN/SET_DUE/NOTE/MARK_STATUS/DRAFT on
      todos and construction items reached via the existing veridian-client.ts assistant
      proxy), and (2) email-as-interface: send.projexa-ai.com / reply.projexa-ai.com
      Resend subdomains, membership-scoped one-click action tokens enforced by a real DB
      trigger (org match, not app-layer check), digest emails off `todos`. Building the
      vertical slice first per the work order's own instruction, not the full surface.
      File scope: new drizzle/ migration(s) for org_ai_link + email_action_token (+
      trigger), src/app/api/ai/[token]/route.ts (new, public), a new proposal-apply
      route, src/lib/email/* (new), src/lib/db/schema.ts (additive only, same convention
      veridianCredentials already uses), a small persistent UI entry point in the app
      shell. NOT touching compliance-tracker's repo or its existing codeReference/
      ai-link/AiConnectorPicker features -- calling through to the existing
      POST /api/v1/projexa/assistant codeReference dispatch for construction-domain data
      rather than duplicating its personal-data boundary. Vercel: `vercel.json`'s
      ignoreCommand (R87, 2026-09-13) already permits a real push to `main` to deploy --
      not blocked by the stale R76 lockdown prose still in this file's CLAUDE.md, which
      documents the pre-R87 state.
  - session_label: "claude-code (task-20260727-122821-projexa-e2e--pwa-offline-sync---real-tas) -- PWA/offline infra + dependency-blocking enforcement"
    claimed_at: "2026-07-27T (this session)"
    claim: |
      Owner directive PROJEXA_ERP_END_TO_END_REQUIREMENT_ANALYSIS_GAP_FILL_AND_IMPLEMENTATION.
      In PROJEXA: adding src/app/manifest.ts (PWA manifest), public/sw.js +
      registration (offline shell caching), and an IndexedDB-backed
      (idb-keyval) offline queue for work-progress entries (quantity/notes/
      photo) with a visible "queued, will sync" UI state syncing to the real
      constructionWorkProgressEntries API. File scope: src/app/manifest.ts,
      public/sw.js, a new client component registering the SW, a new
      offline-queue lib module, work-progress entry UI, e2e/ tests. Not
      touching construction-boq-service.ts or the BoQ schema (separate
      parallel task's scope). In compliance-tracker (separate repo, separate
      claim there): pms-issue-service.ts dependency-blocking enforcement.
  - session_label: "Super Boss (Claude Code, laptop, Sonnet 5) -- R43 seq2: permits.list reads from the screen_definitions registry"
    claimed_at: "2026-08-24T (this session)"
    claim: |
      Sibling of compliance-tracker's ACTIVE-CLAIMS entry with the same
      title -- that repo gets the new resolve-definition.ts + Bearer-key
      route (/api/v1/projexa/screen-definitions/[functionId]); this repo's
      change is the consumer side: src/app/(app)/permits/page.tsx (add a
      callVeridian('/screen-definitions/permits.list') call, server-side,
      passed down as a prop) and src/components/PermitsListClient.tsx
      (accept an optional columns prop from the registry; keep the existing
      hardcoded COLUMNS const as the fallback when the registry call 404s or
      errors, never a hard crash). Not touching permits/[id]/page.tsx or
      permits/new/page.tsx (PERMITS.OBJECT is a separate r43_queue row).
  - session_label: "Claude Opus 5 (laptop) -- R67 programme, workstream J (public-site), branch r67/j-public-site"
    claimed_at: "2026-09-03T (this session)"
    claim: |
      R67 items J-01 (audit R-246), J-02 (R-279) and J-03 (R-280): make the
      two PUBLIC marketing routes static and light. File scope, PROJEXA
      only: src/app/page.tsx, src/app/how-it-works/page.tsx,
      src/app/layout.tsx, src/middleware.ts (the "/" logged-in redirect
      moves here out of the page), src/i18n/request.ts,
      src/i18n/client-messages.ts (new), src/lib/public-page-cache.ts (new),
      next.config.ts (headers()), src/components/marketing/Reveal.tsx, plus
      sibling tests, ai-os/scripts/measure-public-page-transfer.mjs and
      ai-os/audit/public_pages_transfer.json. NOT touching any authenticated
      route, any (app)/ page, veridian-client.ts, the schema, or
      compliance-tracker.

      J-04 (audit R-281) IS SKIPPED, deliberately and with a reason, not
      forgotten: it is measurement on a Vercel PREVIEW DEPLOY, and the
      owner's 2026-09-02 directive forbids any Vercel deploy, build, unpause
      or settings change until credits are bought. It also dependsOn F-32,
      which belongs to another lane. It goes into the post-credits
      measurement backlog and is re-run there; nothing in this branch
      substitutes for it.

      FIX PASS (2026-09-03, same session) widened the file scope, because
      review found the first cut had retired the shipped Hindi marketing
      translation as a side effect of going static. Added: src/i18n/locales.ts
      + messages.ts (new, the vocabulary middleware and request.ts now share
      instead of hand-syncing two copies), src/app/hi/page.tsx and
      src/app/hi/how-it-works/page.tsx (new -- one prerendered document per
      locale), src/components/marketing/* (every Server Component now takes an
      explicit locale), MarketingLocaleProvider.tsx and
      how-it-works/HowItWorksContent.tsx (new), messages/{en,hi}.json (one new
      key for the back link that was hard-coded English JSX),
      src/lib/authz/page-access.ts and src/lib/nav-routes.ts (the two new
      public routes), src/middleware.test.ts (new), playwright.config.ts.
      Still PROJEXA only, still nothing authenticated.
recently_completed:
  - session_label: "claude-code (sub-agent, isolated worktrees C:/ct/ct-worktrees/fix-ai-link-pipeline-tasks + C:/ct/projexa-worktrees/fix-ai-link-pipeline-tasks, branch fix/ai-link-pipeline-tasks-repoint in both repos) -- PROJEXA-E2E-001 work order section 2 item 3 ('AI link'): fix (not just verify) the 2 real, confirmed bugs a sibling verification session found -- Bug 1: ai_link_projection()/apply route operated on the orphaned, UI-dead `todos` table instead of the real, live compliance.pipeline_tasks system (M24 Task Master); Bug 2: the snapshot never gave the reading AI a usable id (only the free-text label) [DONE -- both fixed, live-verified as far as this session's real infra constraints allowed, PRs open/unmerged]"
    claimed_at: "2026-09-21T (this session)"
    completed_at: "2026-09-21T (this session)"
    claim: |
      PRs: FChecklist/compliance-tracker#1781 (new PATCH /api/v1/projexa/
      tasks/[id], restricted to status in {'to_do','done'}) +
      FChecklist/projexa#315 (repoints [token]/route.ts + apply/route.ts to
      pipeline_tasks via VERIDIAN's existing GET /tasks[/[id]], drops the
      TODOS section from ai_link_projection() via new drizzle/0027, adds
      [id: <real id>] rendering to the snapshot). Both left open/unmerged
      per ground rules. public.todos/email_action_token and the email
      digest/reply-parser surface (the sibling 'Email' claim's scope) were
      NOT touched -- confirmed by diff, that feature is untouched.

      REAL FINDING (schema read, src/lib/db/schema.ts's pipelineTasks):
      NO assignee_id, NO due_date, NO note/description column -- only
      `status` is real. Verb-by-verb outcome: MARK_STATUS and DRAFT keep
      working for real; ASSIGN/SET_DUE/NOTE are now honestly refused for a
      pipeline_tasks target (no such field) rather than forced onto one
      that doesn't exist -- exactly the work order's own fallback
      instruction. MARK_STATUS's new PATCH is deliberately restricted to
      'to_do'/'done' (a human sign-off's two plausible meanings) --
      in_progress/waiting/blocked stay executor-exclusive.

      LIVE VERIFICATION, done as far as this session's real, measured
      constraints allowed (not silently skipped, see below):
        1. ai_link_projection() confirmed fixed LIVE via the Supabase MCP
           (evpckeuxgvahguwsaeul) -- now returns ORGANIZATION + RECENT
           NOTIFICATIONS only, no TODOS section. Also confirmed through the
           real, running shared projexa:3100 server (unmodified app code)
           by fetching a real, existing AI Link token for "Meridian
           Construction Group" -- the live response has no TODOS section
           (SQL fix live) and still says "targetKey is the id of a todo
           shown above" (app-code fix not yet live there, as expected --
           proves Bug 2 was real).
        2. A real pipeline_tasks row was created live via the real,
           unmodified POST /api/v1/projexa/tasks while the shared ct:3000
           server was still healthy (id qzsnbaysgi5an90g52s8i5wc, org
           4ecc472f-4152-4310-ae8d-cf8b7c52ab6d / Meridian Construction
           Group).
        3. The shared ct:3000 server then crashed mid-session (repeated
           direct probes returned 500 on `/` itself -- the same documented
           Turbopack/RAM-starvation class this file's own history already
           names, unrelated to this change). This session's own measured
           free RAM (0.05-0.43GB of 7.82GB throughout, driven by other
           parallel sessions' processes) made starting an additional
           isolated dev server unsafe to attempt on a shared machine other
           active sessions depend on -- so this repo's own fixed route code
           (which needs ct:3000 for every VERIDIAN call) could not be
           exercised live over HTTP. Per ground rules, the shared server
           was not restarted.
        4. Substituted, not skipped: the exact UPDATE statement the new
           compliance-tracker PATCH issues was run directly against the
           real row via the Supabase MCP and correctly persisted (to_do
           confirmed, then done); a falsifiability check (same statement,
           wrong org_id) touched ZERO rows. Row left at status='done'.
        5. This repo's route-level logic (parsing, [id: ...] rendering,
           per-verb refusal reasons, org-scoped target lookup) is covered
           by realistic, passing unit tests built against the exact live
           response shapes captured in steps 1-2: 34/34 (--isolate).
        6. Cross-surface Tasks-tab browser screenshot NOT obtained --
           blocked by the same ct:3000 crash (Task Master's GET /api/tasks
           proxies to it) and, independently, by the documented
           storageState-vs-RAM constraint this file's own R75 Part 2
           history already named. Disclosed honestly rather than silently
           skipped or faked.
  - session_label: "claude-code (interactive, sub-agent) -- PROJEXA-E2E-001 section 5 item 7: Site Engineer project lock decision (last of the work order's 8 named broken screens), sibling of /change-orders #299 and /site-diary #301 [DONE, PR #308 open/unmerged pending owner review -- this is a decision point, same posture as /site-materials #304]"
    claimed_at: "2026-09-20T (this session)"
    completed_at: "2026-09-20T (this session)"
    merged_as: "NOT merged -- PR #308 (FChecklist/projexa) left open per the ground rules, pending the owner's explicit sign-off on the recommendation below."
    claim: |
      Owner's exact words: "Site Engineer project lock -- Auto-locks to
      'Business Bay Corporate HQ' instead of the seeded project. Tell me
      plainly: intentional role scoping or a bug?"

      PLAIN ANSWER: neither. It is the SAME generic, role-blind
      alphabetically-first-project fallback already diagnosed by #299
      (/change-orders) and #301 (/site-diary) -- NOT a deliberate
      role-scoping feature (no such mechanism exists anywhere in this app)
      and NOT a role-specific bug (the fallback applies identically to
      every role, confirmed live). No functional fix needed or made.

      EVIDENCE (code + live, not guessed):
      - Grepped "site_engineer" across project-selection.ts,
        project-preference.ts, M24Shell.tsx's chooseProject(), and
        middleware.ts -- zero hits.
      - compliance-tracker's information_schema has zero
        project_assignment/project_member-style table anywhere.
      - GET /projects (v1/projexa/projects/route.ts ->
        listProjectsForSelection, orderBy asc(name)) takes only orgId, no
        role, no membership check beyond org membership itself.
      - LIVE-VERIFIED against the real e2e/users.ts site_engineer account
        (Manoj Yadav, "Meridian Construction Group (E2E Test Org)"): a
        fresh login with no stored `veri.rail.project` preference landed
        /site-diary on projectId=db0i9peek6gbjfltlfmduu7u
        ("E2E-BatchA-1784446620540 Test Project"), confirmed via a direct
        /api/projects fetch to be the alphabetically-first ACTIVE project in
        this org -- exactly the documented "auto" fallback class
        (PHASE2_BATCH_B_FINDINGS.md's own "Batch A test-project pollution"
        note), nothing role-specific.
      - The owner's own "Business Bay Corporate HQ" observation (a
        different org, projexa_demo_org) is the identical mechanism:
        confirmed via Supabase query that "Business Bay Corporate HQ - Full
        Renovation" sorts alphabetically before "Villa 21 - Whitefield"
        (the richly-seeded project).
      - Confirmed the switcher is NOT locked for this role: switching
        Manoj Yadav's project via the top-rail switcher updates immediately
        and the choice survives a bare re-navigation to a different module
        (/work-progress, no ?projectId=) -- identical to PR #305's proof for
        the `ceo` role on /schedule. Falsifiability-checked for real: broke
        all three of this app's cookie-read paths (project-preference.ts's
        readStoredProjectId, project-selection.ts's readPreferredProjectId,
        module-list-source.ts's resolveProjectIdFastWithSource) at once,
        confirmed the persistence test correctly FAILED (reverted to the
        junk auto-selected project, log-verified via
        projectId=db0i9peek6gbjfltlfmduu7u reappearing), then restored all
        three (git diff empty before commit).

      BUILT: e2e/site-engineer-project-switcher.spec.ts (2 real Playwright
      tests, run against local bun dev :3100 + compliance-tracker local dev
      :3000 + live Supabase, not production) -- proves the Site Engineer
      role can deep-link to ANY project in the org (not restricted to one)
      and that the switcher's choice persists exactly like every other
      role. No application code changed -- this is confirm-and-close, not a
      fix, matching the outcome #299's own PR description already reached
      for /change-orders.

      File scope: e2e/site-engineer-project-switcher.spec.ts (new),
      ai-os/boss/ACTIVE-CLAIMS.yaml. Worked in isolated worktree
      C:\ct\projexa-siteeng-fix (branch fix/site-engineer-project-lock),
      NOT the shared C:\ct\projexa checkout, to avoid the concurrent merge
      campaign running there. Backend verified read-only via a second
      isolated worktree, C:\ct\ct-siteeng-verify (compliance-tracker,
      detached at origin/main) -- no compliance-tracker file was changed.

      STILL NEEDS THE OWNER'S SIGN-OFF: not because a product decision is
      pending here (there is nothing to decide -- there is no role-scoping
      feature to keep or remove), but because this closes out a named item
      the owner explicitly asked to be told "plainly," same posture as
      #304 (/site-materials).
  - session_label: "claude-code (sub-agent, isolated worktree C:\ct\projexa-schedule-fix, branch fix/schedule-project-switcher) -- PROJEXA-E2E-001 section 5 item 6: /schedule ignores the global project switcher, reads only ?projectId= [DONE. PR FChecklist/projexa#305 open, not yet merged -- left open/unmerged per the owner's ground rules for this work order]"
    claimed_at: "2026-09-20T (this session)"
    completed_at: "2026-09-20T (this session)"
    claim: |
      Root cause confirmed by reading the code (not assumed from the owner's
      description): /schedule/page.tsx was the ONLY project-scoped screen
      that called resolveRouteProject() (src/lib/project-selection.ts -- the
      A-13 "strict URL-only, no cookie fallback" resolver), while every other
      project-scoped page calls resolveSelectedProject()/resolveProjectForModule(),
      both of which also honour the rail's own veri.rail.project cookie.
      M24Shell.tsx's chooseProject() (the global switcher's handler) only
      pushes the new id into the URL when the CURRENT url already carries a
      ?projectId= (routeProjectId truthy); otherwise it writes the cookie and
      calls router.refresh() -- which works for the ~50 cookie-aware pages but
      was a no-op for /schedule, which never read that cookie. So landing on
      /schedule with no ?projectId= (fresh nav, bookmark, typed URL) and then
      using the top-rail switcher visibly did nothing.

      FIX: pickRouteProject() (src/lib/project-preference.ts) gains a
      `preferred` fallback tier -- the rail's own remembered cookie choice,
      exactly what pickProject() already grants the other ~50 pages --
      inserted between "nothing named" and "ask", and resolveRouteProject()
      threads readPreferredProjectId() into it. The first-project GUESS A-13
      exists to forbid stays forbidden (pickRouteProject still never reaches
      projects[0]); only the "did the user already tell us, via the switcher"
      tier is added. Explicit ?projectId= deep links still outrank the
      cookie outright, so shared/deep links are unaffected.

      VERIFIED LIVE: both dev servers (ct :3000, projexa :3100) against live
      Supabase pcrjmlpuqsbocqfwoxod, logged in as the committed `ceo` e2e
      test account -- clicked the switcher between two real projects and
      confirmed /schedule's content updated both while ?projectId= was
      already in the URL and on a bare re-navigation to /schedule
      afterward. FALSIFIABILITY CHECKED: reverted just the fix files,
      re-ran the new e2e spec, confirmed the regression assertion fails
      exactly as expected ("Pick a project" reappears); restored the fix
      and it passed again.

      Tests: project-preference.test.ts (+9 cases), project-selection.test.ts
      (+5 cases, resolveRouteProject end-to-end with a mocked next/headers
      cookie), e2e/schedule-project-switcher.spec.ts (new). Full suite:
      4298/4299 (the 1 failure, dashboard-hierarchy-no-ffe-redirect.test.ts,
      is pre-existing/unrelated -- passes clean in isolation, touches none of
      these files). typecheck + lint clean. File scope: src/lib/project-
      preference.ts(+.test.ts), src/lib/project-selection.ts(+.test.ts),
      src/app/(app)/schedule/page.tsx (comment only), e2e/schedule-project-
      switcher.spec.ts (new). Does not touch change-orders/site-diary/
      reports/scope (siblings already fixed/claimed elsewhere in this same
      work order). Checked platform.sumeet_requirements for schedule/timeline
      rows (R-C10, R-94) -- both already CLOSED on unrelated evidence
      (schedule/milestone CRUD persistence, not project resolution); left
      untouched per this task's own "don't touch unrelated rows" instruction.
  - session_label: "claude-code (sub-agent, PROJEXA-E2E-001 sub-task) -- investigate /site-materials silent redirect to /materials, decide real-screen-vs-remove (work order section 5 item 5) [DONE -- recommendation: remove, implemented on branch, PR left OPEN/UNMERGED per owner's ground rules, needs owner sign-off since it is a product-scope decision]"
    claimed_at: "2026-09-20T (this session)"
    completed_at: "2026-09-20T (this session)"
    claim: |
      Investigated the owner's explicit ask ("real screen, or remove the route?")
      rather than treating it as a bug fix. Evidence found: (1) compliance-tracker's
      own schema.ts (construction_materials/construction_material_receipts) is
      already project/site-scoped with no org-wide master to distinguish a "site"
      view from -- the service layer's own comment says so directly; (2)
      platform.sumeet_requirements carries exactly one row for this area (R-C08,
      "Material database, material inbound, spec, cost, qty"), treating it as one
      concept; (3) no crr_ruling/claude_log entry anywhere discusses "site
      materials" as intentionally distinct; (4) /site-materials's own 2026-08-30
      history already found its Catalog tab was the identical constructionMaterials
      table under a second label, Inbound/Cost-Report tabs dead since R52. That
      session redirected but kept a duplicate sidebar entry as a defensive measure
      against an unrelated, no-longer-applicable regression -- leaving two nav
      items ("Materials"/"Site Materials") pointing at the same screen, one via an
      unexplained teleport, which is exactly the redundant UX the owner flagged.

      Recommendation: remove, not redirect. Implemented on an isolated worktree
      (C:\ct\projexa-sitematerials-fix, branch fix/site-materials-decision, off
      origin/main @ eb33f85): deleted src/app/(app)/site-materials/page.tsx;
      removed the route from nav-routes.ts's SHIPPED_ROUTES and the nav entry from
      AppSidebar.tsx; removed the /site-materials prefix from module-catalogue.ts;
      removed the siteMaterials i18n key from messages/en.json + hi.json; flipped
      nav-routes.test.ts's R52 regression guard to assert /site-materials stays
      gone (was asserting it stays in nav) so it can't be silently reintroduced.

      Verified: tsc --noEmit clean, eslint clean (2 pre-existing unrelated
      warnings only), full `bun test --isolate` 4289/4289 pass, `bun run build`
      succeeds with /site-materials confirmed absent from the route manifest. PR:
      FChecklist/projexa#304. Left OPEN/UNMERGED per the owner's ground rules for
      this work order (never merge to main) -- explicitly flagged in the PR
      description as needing the owner's own sign-off since this is a
      product-scope decision (remove vs. build a distinct screen), not a pure
      correctness fix. Did not touch compliance-tracker (no backend change
      needed -- /materials' own Cost Report tab already owns the one real
      backend endpoint this domain uses).
  - session_label: "claude-code (sub-agent, PROJEXA-E2E-001 sub-task) -- /scope Filter+Export marked 'Not yet available' (work order section 5 item 4) [DONE, PR left OPEN/UNMERGED per owner's ground rules]"
    claimed_at: "2026-09-20T (this session)"
    completed_at: "2026-09-20T (this session)"
    claim: |
      Isolated worktree C:\ct\projexa-scope-fix, branch fix/scope-filter-export, off
      origin/main @ eb33f85 -- NOT working in the shared C:\ct\projexa checkout
      (several parallel sessions use it) or C:\ct\ct (backend not touched;
      read-only local dev server for live verification, no schema/service change).

      Confirmed by reading src/components/ScopeClient.tsx directly: Filter and
      Export were genuinely UNBUILT stub buttons (`disabled title="Not yet
      available"`, no onClick, no data wired to either) -- not a bug masking a
      silent failure, unlike the /change-orders and /site-diary findings earlier
      in this work order. Built the real, minimal, working version, reusing this
      repo's own established patterns: the Filter/Export header trio + inline
      filter bar (Status + Title, client-side over the rows already on screen,
      "Showing n of m", Clear all, a distinct filtered-empty-state) from
      DocumentsClient.tsx (R67 D-14), and the CSV builder from
      src/lib/csv-export.ts (toCsv/downloadCsv, already used by
      BudgetAnalyticalClient/DocumentsClient/LabourClient/etc.). Filtering
      happens on the flat BOQ list before lineage grouping (buildLineageRows),
      so a filtered-out revision does not drag its whole lineage along.

      Live-verified against the real Supabase project (pcrjmlpuqsbocqfwoxod) via
      local bun dev on both repos, logged in as the real, already-committed E2E
      test CEO account (projexa's e2e/users.ts, "Meridian Construction Group
      (E2E Test Org)" -- no password set/reset/touched, per this work order's
      credential-safety rule): on /scope for "Meridian Heights - Residential
      Tower A" (2,467 real BOQ rows), Status=draft narrowed to 2,068 records in
      real time; Title="R-22" narrowed to 130 real matching rows; a combined
      non-matching filter correctly rendered the real "No BOQs match this
      filter" empty state with a working Clear filters action; Export with the
      R-22 filter active downloaded a real CSV whose captured content (131
      lines: 1 header + 130 data rows) matched the on-screen "Showing 130 of
      2467" exactly, with real field values from the live BOQ data.

      File scope: src/components/ScopeClient.tsx, src/components/
      ScopeClient.test.tsx (30 pass, bun test --isolate; new pure-function
      coverage for applyScopeFilters/hasActiveScopeFilter/knownScopeStatuses,
      an interactive Status-driven render test, and an Export test that
      captures a real downloaded CSV's exact content). Did NOT touch
      ScopeObjectClient.tsx, ScopeCompareClient.tsx, ScopeCreateClient.tsx,
      ScopeImportClient.tsx, ScopeReviseClient.tsx, or any compliance-tracker
      file (the BOQ list API already returns everything this filter/export
      needs). One documented, environment-only test gap: the Title text
      input's own onChange is not driven via fireEvent in the render suite --
      a pre-existing, already-recorded limitation of this repo's test
      environment (React 19 + happy-dom under bun test; see
      BudgetAnalyticalClient.test.tsx's own comment) -- covered instead by the
      pure applyScopeFilters function plus the real-browser verification above.

      PR: FChecklist/projexa#303, commit 46d1107. CI green (Lint/Type
      Check/Test 30 pass/Build/Migration guard/Secret Scanning); Vercel check
      shows "blocked" by design (R87 ignoreCommand gate on a non-main branch),
      not touched. platform.sumeet_requirements row R-96 updated with an
      addendum (real, verified new information; its own naming-caveat closure
      is unrelated and untouched).
  - session_label: "claude-code (sub-agent, PROJEXA-E2E-001 sub-task) -- re-investigate /change-orders 'No change orders yet' (work order section 5 item 1) [DONE, work complete -- PR left OPEN/UNMERGED per owner's ground rules, pending the owner's batch merge]"
    claimed_at: "2026-09-20T (this session)"
    completed_at: "2026-09-20T (this session)"
    claim: |
      Reconciled R-97's 2026-09-18 close (claimed zero seeded change orders
      for the fallback project) against the Owner's fresh 2026-09-20
      screenshot (claims 2 real change orders exist and aren't rendering) --
      NOT a contradiction, both are the same underlying mechanism. Verified
      via compliance-tracker's listChangeOrders() service called directly
      (bypassing HTTP/auth) against org=projexa_demo_org/
      project=projexa_demo_project: the query genuinely returns both real
      rows, so the data/service/route layers were never at fault. Real root
      cause: change-orders/page.tsx never named which project it was
      showing, so resolveSelectedProject()'s documented first-project
      (alphabetically-first) fallback could silently land any fresh visitor
      on "Business Bay Corporate HQ" (0 seeded change orders) instead of
      "Villa 21 - Whitefield" (2 real ones), with the empty state giving no
      indication a different project even existed. Fixed by adopting the
      already-shipped Documents/Labour/Drawings/MoMs convention (R67
      D-13/D-20/D-32): name the resolved project + admit "(auto-selected)".
      File scope: src/app/(app)/change-orders/page.tsx,
      src/components/ChangeOrdersClient.tsx, new
      src/components/ChangeOrdersClient.test.tsx (4 cases,
      falsifiability-checked). PR: FChecklist/projexa#299, commit
      a9fa2aeb2cbab93503be3229a7aa7e8bcf3058ac. CI green (Lint/Type
      Check/Test 193 pass/Build/Migration guard/Secret Scanning); Vercel
      check shows "blocked" by design (ignoreCommand gate on a non-main
      branch), not touched. platform.sumeet_requirements row R-97 updated
      with this finding (real, verified new information; not touched
      blindly). Did NOT touch ProjectWorkspaceClient.tsx's own
      ChangeOrdersClient call site (the "Merge 6" workspace page a
      different active session owns per this file's `active:` section) --
      the new props are optional so it compiles unchanged.
  - session_label: "claude-code (sub-agent, PROJEXA-E2E-001 sub-task) -- e2e timeout sweep: 22 files with 10-15s explicit timeouts on individual expect/poll calls, found via a raw grep during the R-95 billing-milestones closure (PR #297)."
    claimed_at: "2026-09-20T (this session)"
    completed_at: "2026-09-20T (this session)"
    claim: |
      Read each of the 22 grep hits (`grep -rl "timeout: (1[0-5])_000" e2e/`)
      in context (not a blanket find-and-replace) and raised 79 call sites
      across 16 files to 30_000ms where the timeout guarded a real
      GET/POST/PATCH round trip through the VERIDIAN-proxy path, each with
      its own comment citing the specific round trip covered -- matching
      playwright.config.ts's own actionTimeout/navigationTimeout (already
      30_000ms) and the precedent PR #297 established for the sibling
      sumeet-billing-milestones-env1.spec.ts. Left 6 files/10 call sites
      unchanged with reasoning recorded at each site: 12-member-access.spec.ts
      (auth redirect, not a data round trip), pivot-chart-reports.spec.ts
      (client-side tab-switch re-render, no new fetch),
      r80-r81-r82-copilot-pill-chain-env1.spec.ts (deliberately tolerant
      `.catch(() => {})` sweep-loop probe), r81-d603-scope.spec.ts
      (pure client-side computed state), r91-cold-start-failed-fetch-env1.spec.ts
      (deliberately-aborted, instantaneous synthetic network failure),
      rb2-boq-create-real-click-and-progress-dropdown-env1.spec.ts's own
      listbox-visible line (renders synchronously on focus, confirmed via
      source), and sumeet-exceptions-env1.spec.ts (local React state toggle
      over already-fetched data, confirmed via ExceptionsClient.tsx).
      sumeet-billing-milestones-env1.spec.ts itself untouched -- that fix is
      the separate, already-open PR #297. Worked in an isolated worktree at
      C:\ct\projexa-worktrees\wo-r95-e2e-timeout-audit, branch
      wo/r95-e2e-timeout-audit, off origin/main. PR opened:
      https://github.com/FChecklist/projexa/pull/298 -- left OPEN/UNMERGED
      per the owner's PROJEXA-E2E-001 ground rules (local dev + GitHub CI +
      live Supabase only, no Vercel; merges are the owner's own call).
  - session_label: "claude-code (sub-agent, PROJEXA-E2E-001 sub-task) -- R-95 billing-milestones closure re-audit (platform.sumeet_requirements, id='R-95') [DONE, closed with a real green CI run; PR left open/unmerged: #297]"
    claimed_at: "2026-09-20T (this session)"
    completed_at: "2026-09-20T (this session)"
    claim: |
      Root cause: NOT a backend defect (compliance-tracker's boq-analysis-
      service.ts/construction-boq-service.ts verified correct via code
      review + a real local create->submit->approve->poll run against the
      live pcrjmlpuqsbocqfwoxod Supabase project). This spec's own timeouts
      (15s/10s waits, 75s whole-test budget) were tighter than this
      program's own established CI-latency standard (playwright.config.ts's
      30s actionTimeout/navigationTimeout), compounded by compliance-
      tracker's extensively-documented "E2E Tests (Env-1, cross-repo)" job
      latency history.

      Fixed across 3 commits on branch wo/r95-billing-milestones-boq-
      analysis-poll (ba5c900, 2b77541, e39f543): raised the boq-analysis
      poll (15s->30s), 5 other network-dependent step timeouts in the same
      spec (10-15s->30s), and the test-level budget (75s->120s,
      proportionate to its own 6-step chain). No assertion logic loosened.
      Each fix verified against a REAL, isolated GitHub Actions dispatch
      (compliance-tracker's ci.yml workflow_dispatch, projexa_ref +
      projexa_spec_grep inputs targeted at this branch/spec) before the
      next -- 3 real CI failures hit and fixed one at a time (Drafted-badge
      timeout, a transient BOQ-creation failure, then the cumulative
      whole-test-timeout) before a genuine green run: compliance-tracker
      run 35499299260, "E2E Tests (Env-1, cross-repo)" job, "Run projexa
      Env-1 spec(s)" step: 6 passed including the real target test.

      platform.sumeet_requirements R-95: closure_state=CLOSED,
      closure_commit_sha=e39f5437673554c1010338b86b369690a6e2d163,
      closure_repo=projexa, closure_ci_run_id=35499299260 -- full
      root-cause/fix history recorded in that row's own status field.

      PR: FChecklist/projexa#297, left open/unmerged per the owner's
      PROJEXA-E2E-001 ground rules (owner does one batch merge later,
      backend-first then frontend). Worked in an isolated worktree
      (C:\ct\projexa-worktrees\wo-r95-billing-milestones) rather than the
      shared C:\ct\projexa checkout, which another PROJEXA-E2E-001 sub-task
      (R-B2) had checked out to its own branch at the time -- avoided
      disrupting it. Local dev servers stopped, worktree left in place for
      any follow-up.
  - session_label: "claude-code -- PROJEXA real search + notifications (owner-directed gap closure of the searchSlot/notificationSlot honestly-disclosed-omission from PR #42/#43)"
    claimed_at: "2026-07-19T (this session)"
    completed_at: "2026-07-19T (this session)"
    claim: |
      Built real command-palette search (src/components/search-command.tsx +
      src/app/api/search/route.ts, src/lib/services/search-service.ts) across
      PROJEXA's real entities: org projects, the current project's real
      VERIDIAN-backed RFIs/Submittals/Punch List items/Change Orders (fetched
      live via the existing veridian-client.ts proxy, filtered in
      application code -- these entities live in VERIDIAN, not PROJEXA's own
      Postgres, so there is no ILIKE path for them), and PROJEXA's own local
      `todos` table (real Postgres ILIKE). Built a real `notifications`
      table (drizzle/0011_notifications.sql) + API routes
      (src/app/api/notifications/route.ts,
      src/app/api/notifications/[id]/read/route.ts) + bell dropdown
      (src/components/NotificationBell.tsx), wired to 3 real existing
      call sites: RFI created (src/app/api/rfis/route.ts POST), submittal
      status changed (src/app/api/submittals/[id]/route.ts PATCH), punch
      list item created (src/app/api/punch-list/route.ts POST) -- each
      fans out to the org's other real members via the existing
      `memberships` table. Wired both into AppTopbar.tsx's searchSlot/
      notificationSlot (previously `false`). File scope: the files listed
      above, src/lib/db/schema.ts, drizzle/0011_notifications.sql, this
      claims file, PROGRESS.md. Did not touch compliance-tracker or
      veridian-ui-kit. TIER2 (new migration/schema) -- not self-merged,
      reported for Owner sign-off. See the PR description for the exact
      migration-apply caveat (no live DB credentials in this environment;
      the .sql file is the durable record per this repo's own established
      convention, same as drizzle/0008 and 0009 -- someone with Supabase
      MCP/DB access still needs to apply it before the feature is live).
